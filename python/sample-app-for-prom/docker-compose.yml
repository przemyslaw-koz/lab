services:
  sample-app-for-prom:
    build: .
    container_name: sample-app-for-prom
    ports:
      - "5000:5000"
      - "5001:5001"
    environment:
      HOST: "0.0.0.0"
      PORT: "5000"
    restart: unless-stopped
